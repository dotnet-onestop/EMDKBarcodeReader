repositories{    
  jcenter()
  flatDir {
      dirs 'emdk'
   }
}

dependencies {
 provided fileTree(include: ['com.symbol.emdk.jar'], dir: 'emdk')
  compile 'com.android.support:appcompat-v7:23.2.0'
   
}

